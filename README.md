
# ERIS Technology — React Demo

A small React demo matching the SRS: Home, Services (with CTAs), About, and Contact/Enquiry.

## Run locally (Node 18+)
```bash
npm install
npm run dev
```
Open the URL Vite prints (e.g., http://localhost:5173).

## Build & Preview
```bash
npm run build
npm run preview
```

## Notes
- Form submission shows a demo alert (no backend).
- Replace colors/logo and connect a real API when ready.
```
